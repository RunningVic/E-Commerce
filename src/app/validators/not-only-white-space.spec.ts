import { NotOnlyWhiteSpace } from './not-only-white-space';

describe('NotOnlyWhiteSpace', () => {
  it('should create an instance', () => {
    expect(new NotOnlyWhiteSpace()).toBeTruthy();
  });
});
